# Node / Playwright
node_modules/
playwright-report/
test-results/
.playwright/

# Demo capture artifacts (regenerated; don't commit takes)
demo-out/
*.raw.mov

# Claude Code — personal, machine-local overrides (never shared)
.claude/settings.local.json
